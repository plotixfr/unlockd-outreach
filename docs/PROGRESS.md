# Session progress — go-live tomorrow (2026-06-14)

> Resume rule: read THIS file + `git log` first; resume from the first incomplete phase.
> Prior session (hotfix/replies/redesign/P1, 2026-06-12) is complete — see git history `5843a9c`…`a66e74a` + the email-copy commit `24cb8f0`. This session's baseline HEAD = `24cb8f0`.
> Global rules in force: never print secrets; credential rotation deferred (closed); never weaken Bearer/session auth; redrive cap 5/tick stays; prospect copy FR/NL, admin English; `vercel` via npx (sensitive prod vars pull as "" → verify functionally); never commit `scripts/backups/`; one commit per unit, tsc+tests+lint green first; NO full `next build`.

| Phase | Scope | Status | Evidence / commit |
|---|---|---|---|
| 0 | Env sanity — IMAP_USER/PASSWORD/CRON_SECRET exist; IMAP_USER non-sensitive; .gitignore covers .env*, no env tracked | ✅ DONE | IMAP_USER present & NON-sensitive (pulls as temim@unlockd.art, len 17), single row (not dup) → no fix. IMAP_PASSWORD + CRON_SECRET sensitive ✓. `.gitignore` `.env*` ✓, 0 env files tracked. FLAG: RESEND_WEBHOOK_SECRET ABSENT in prod (→ Phase 4 bounce). |
| 1 | Claude credits proven functionally (1 minimal claude-sonnet-4-6 call); distinct API_CREDIT_OR_AUTH_ERROR signature so billing never masquerades as gen bug | ✅ DONE | `e31fa0d`. Live probe: OK (claude-sonnet-4-6, in=8 out=1) → credits healthy. classifyClaudeError() (401/402/403/429 + credit/billing/quota msg) wired into scoreProspect (re-throws), autopilot scoring+generateEmailsInline (distinct failure), /api/emails/generate (402). |
| 2 | Disable site-preview in send flow (flag OFF), map all gen/attach/ref, no orphaned refs across all 6 send paths | ✅ DONE | `f7617c5`. ENABLE_SITE_PREVIEW flag (src/lib/flags.ts, default OFF). Map: mockup.ts(gen), autopilot MOCKUP_ENABLED, emailPrompt buildFactsBlock mockupUrl blocks + 2 follow2 lines, sendEmail thum.io screenshot + F2 "direction visuelle". Proven: OFF→0 preview-image refs (FR+NL), ON→refs return. /preview never linked from email. thum.io current-site screenshot ALSO gated (criterion: no inline image) — FLAGGED for user. tsc+25 tests+lint ✓. |
| 3 | IMAP host config + LIVE connection test (Titan) + reply routing + backfill-before-unfreeze | ✅ DONE (code; auth test handed to user) | Code already correct (`9cf2755`): IMAP_HOST default imap.titan.email:993 TLS, env-overridable; REPLY_TO=IMAP_USER✓; backfill-before-unfreeze gate (sendEmail.ts:615-655 — follow-ups run only after scan.ok, backlog first). LIVE connection probe: BOTH imap.titan.email + imap0101.titan.email return valid IMAP greeting (AUTH=PLAIN) on 993. Persisted **IMAP_HOST=imap.titan.email** in prod (vercel env add). AUTH test: I lack IMAP_PASSWORD locally (sensitive, absent from .env.local) + pasting = secret exposure → wrote `scripts/imap-auth-test.ts` (one attempt, no retry, host-fallback on CONN fail not AUTH fail) for USER to run in own terminal. SAFE either way: gate keeps follow-ups frozen until a real authenticated scan succeeds. 3-point checklist in report. |
| 4 | Unattended-run cron health check; confirm prior fixes intact; 2 Sirene briefs + json-parse; bounce webhook + rate | ✅ DONE | `9dbc51d`. ALL 8 crons strict-bearer ✓ (autopilot/reengage/upsell POST were hardened — APP_MAP §11.2 stale). maxDuration=300s everywhere → **Pro plan confirmed**. Prior fixes ALL intact: shared 230s deadline + gen start-guard (no 504s), redrive cap=5, dash filter autopilot inline, follow-up gate w/ backfill-before-unfreeze (sendEmail.ts:622-649). Minimal fixes: check-replies notifyHotReply .catch; reengage/upsell handler try/catch; inbound-nurture (was FAIL) per-candidate guard + non-fatal post-send persist. FLAGS: (a) **Sirene found:0 ALL industrial briefs** — API returns 1262 HVAC/Lyon cos but 0 have site_internet; adapter drops no-site rows → product decision; (b) **RESEND_WEBHOOK_SECRET ABSENT → bounce webhook 500s every event → bounces NEVER suppressed**; 0% DB bounce rate is meaningless; compounds implicit-MX optimistic-send; (c) 4 prospects `generate: json parse failed` → redrive self-heals (cap 5). DB: 73 prospects (Emailed 26 / New 45 / Sched 1 / Follow1 1), 28 sent. |
| 5 | Final report | ⏳ PENDING | |

## Notes / decisions (this session)
- (populated as phases complete)
